<h1>Lists#index</h1>
<p>Find me in app/views/lists/index.html.erb</p>

<ul>
  <% @lists.each do |list| %>
<!-- .name can be accessed as restaurant data is in a hash format -->
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>
